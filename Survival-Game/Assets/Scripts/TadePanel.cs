using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TadePanel : MonoBehaviour
{
    // Start is called before the first frame update
    public bool on;

    void Start()
    {
        on = false;
        gameObject.SetActive(false);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
